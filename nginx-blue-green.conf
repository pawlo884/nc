# Blue-Green Deployment Configuration for NGINX

# Upstream dla Blue environment
upstream backend_blue {
    server web-blue:8000 max_fails=3 fail_timeout=30s;
}

# Upstream dla Green environment
upstream backend_green {
    server web-green:8000 max_fails=3 fail_timeout=30s;
}

# Aktywny backend - zmień tutaj aby przełączyć między blue/green
# include /etc/nginx/state/active_backend.conf;
# Domyślnie używamy blue
upstream backend_active {
    server web-green:8000 max_fails=3 fail_timeout=30s;
}

# Main server block
server {
    listen 80;
    server_name _;
    client_max_body_size 10M;

    # Logowanie
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # Health check endpoint (nie przechodzi przez Django)
    location /nginx-health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }

    # Blue/Green status endpoint
    location /deployment-status {
        access_log off;
        default_type application/json;
        return 200 '{"active":"blue","timestamp":"$time_iso8601"}';
    }

    # Static files
    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Media files
    location /media/ {
        alias /app/media/;
        expires 7d;
        add_header Cache-Control "public";
    }

    # Flower (Celery monitoring)
    location /flower/ {
        proxy_pass http://flower:5555/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        proxy_redirect off;
        proxy_buffering off;
    }

    # Django application - route to active backend
    location / {
        proxy_pass http://backend_active;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Deployment-Color "green";
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
        
        # Buffering
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
        proxy_busy_buffers_size 8k;
        
        # Disable response buffering for SSE/streaming
        proxy_http_version 1.1;
        proxy_set_header Connection "";
    }

    # Error pages
    error_page 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}

# Admin endpoint for switching between blue/green (internal only)
server {
    listen 8080;
    server_name _;
    allow 127.0.0.1;
    allow 172.16.0.0/12;  # Docker networks
    deny all;

    # Switch to blue
    location /switch/blue {
        access_log off;
        default_type application/json;
        return 200 '{"action":"switch","target":"blue","status":"ok"}';
    }

    # Switch to green
    location /switch/green {
        access_log off;
        default_type application/json;
        return 200 '{"action":"switch","target":"green","status":"ok"}';
    }

    # Status
    location /status {
        access_log off;
        default_type application/json;
        return 200 '{"active":"blue","blue":"healthy","green":"healthy"}';
    }
}

