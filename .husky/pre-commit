#!/usr/bin/env sh

# Nie uruchamiaj hooka w CI (semantic-release wykonuje commit w Actions)
if [ -n "$CI" ]; then
  exit 0
fi

# Lekki check lokalnie zamiast testów (bo brak skryptu test)
npm run lint || npm run format:check || true
