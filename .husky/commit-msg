#!/usr/bin/env sh

# Nie uruchamiaj commitlint w CI (semantic-release tworzy commity)
if [ -n "$CI" ]; then
  exit 0
fi

# Husky shim (zapewnia poprawne zmienne i PATH)
if [ -f "$(dirname -- "$0")/_/husky.sh" ]; then
  . "$(dirname -- "$0")/_/husky.sh"
fi

npx --no commitlint --edit "$1"
